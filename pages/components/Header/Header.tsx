import styles from "./Header.module.scss"

const Header = () => {
    return(
        <header className={styles.headerSection}>
            Header Section
        </header>
    )
}

export default Header;